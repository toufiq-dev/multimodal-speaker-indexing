# Install with the NumPy ABI constraint applied:
#   pip install -c constraints.txt -r requirements.txt
#
# Then run the ORT hygiene step (see kaggle_setup.py): chromadb hard-depends on
# the CPU `onnxruntime` wheel, which shares onnxruntime-gpu's import path and
# silently removes CUDAExecutionProvider.

# Core ML Framework (CUDA 12.1)
# --extra-index-url is ADDITIVE. `--index-url` is a global option in a
# requirements file, so declaring it twice made the last one win for the whole
# file and `torch==2.5.1+cu121` (a local version, never published to PyPI)
# resolved against pypi.org and failed with "No matching distribution found".
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
torchvision==0.20.1+cu121

# NumPy FIRST — locks ABI for insightface, onnxruntime, tokenizers (must be before Vision)
numpy==1.26.4

# Audio Processing
pyannote-audio==3.3.2
faster-whisper==1.1.1
# Pinned explicitly: faster-whisper only requires >=4.0,<5, and CTranslate2
# >=4.5 links cuDNN 9 while <=4.4 links cuDNN 8. torch cu121 ships cuDNN 9.1,
# so an unpinned resolve can produce "Could not load library libcudnn_ops_infer.so.8".
ctranslate2==4.5.0
librosa==0.10.2.post1

# Transformers & NLP
transformers==4.44.0
huggingface-hub==0.25.2
tokenizers==0.19.1
accelerate==0.33.0
sentencepiece==0.2.0
hf-transfer==0.1.9

# Vision (InsightFace requires numpy<2.0)
# insightface 0.7.3 is source-only and its setup.py imports Cython/NumPy, which
# pip's build isolation does not provide. kaggle_setup.py installs it with
# --no-build-isolation after seeding the build deps.
insightface==0.7.3
opencv-python-headless==4.10.0.84
scikit-learn==1.5.1
scikit-image==0.24.0

# Face Analysis Dependencies
onnxruntime-gpu==1.19.2

# YOLO-face ablation (engines/vision_yolo.py). Without it the ablation raises
# rather than silently re-running RetinaFace as a fake second arm.
ultralytics==8.3.49

# Clustering & Metrics
scipy==1.13.1

# Data Processing
pandas==2.2.2
tqdm==4.66.4
pyyaml==6.0.1

# Utilities
ffmpeg-python==0.2.0
requests==2.32.3
python-dotenv==1.0.1

# LoRA / PEFT (QLoRA 4-bit quantization needs bitsandbytes)
peft==0.12.0
bitsandbytes==0.43.0

# Protobuf compatibility (required by Google Cloud libs, transformers)
protobuf==5.29.3

# FSSpec compatibility
fsspec==2025.3.0

# Rich for progress bars
rich==13.9.4

# Jupyter/Notebook (for Kaggle/Colab execution)
ipykernel==6.29.5
jupyter-client==8.6.2

# Download utilities - pin to existing PyPI version (avoid future 404)
yt-dlp==2024.12.23

# Evaluation - FIXED: 3.3.2 doesn't exist, use 3.2.1
pyannote.metrics==3.2.1
jiwer==3.0.4

# RAG (required post-pipeline stage; engines/rag.py)
sentence-transformers==3.3.1
chromadb==0.5.15
