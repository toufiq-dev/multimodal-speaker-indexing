# Data directories (registry jpg allowed for reproducibility; input/output ignored)
data/input/*
data/output/*
data/registry/*
!data/registry/*.jpg
!data/registry/*.jpeg
!data/registry/*.png
!data/registry/*.webp

# Media files (except registry)
*.mp4
*.wav
*.jpg
*.jpeg
*.png
*.mp3
*.m4a
!data/registry/*.jpg
!data/registry/*.jpeg
!data/registry/*.png
!data/registry/*.webp

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so

# Virtual environments
venv/
env/
.env
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Model checkpoints
*.pt
*.pth
*.bin
*.safetensors

# Converted CT2 models. Never ship a PARTIAL model directory: config.json and
# vocabulary.json are not matched by *.bin, so the clone used to carry a CT2
# folder without weights, which faster-whisper reports as an opaque loader
# error rather than "weights are missing".
models/

# Scratch intermediates (frames, extracted audio)
data/scratch/

# Kaggle
kaggle.json
*.kaggle

# Jupyter
.ipynb_checkpoints/

# Distribution
dist/
build/
*.egg-info/

# Claude
CLAUDE.md